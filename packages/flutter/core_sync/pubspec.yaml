name: core_sync
description: Drift cache schema, outbox worker, and sync controller.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  drift: ^2.22.1
  flutter_riverpod: ^2.5.1
  path: ^1.9.0
  path_provider: ^2.1.4
  sqlite3_flutter_libs: ^0.5.28
  core_models:
    path: ../core_models
  core_network:
    path: ../core_network

dev_dependencies:
  build_runner: ^2.4.13
  drift_dev: ^2.22.1
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  sqlite3: ^2.4.6
