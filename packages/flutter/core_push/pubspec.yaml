name: core_push
description: FCM permission, token registration, and tap routing for both apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.5
  core_auth:
    path: ../core_auth
  core_models:
    path: ../core_models
  core_network:
    path: ../core_network

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  dio: ^5.7.0
